namespace Todo.Web.Client;

// this class by convention by the source generator to know how to include
// the css classes. It'll add a CssClassValues() method that returns all the
// css classes discovered during the source generation phase.
public partial class Monorail
{
    // any calls, by convention, to CssClass will also be included in the source generator. 
    public static string CssClass(string s) => s;
}